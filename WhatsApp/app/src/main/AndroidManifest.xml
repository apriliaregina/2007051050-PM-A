<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whatsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatsApp">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GrupActivity"
            android:label="Grup baru" />
        <activity
            android:name=".SiaranActivity"
            android:label="Siaran baru" />
        <activity
            android:name=".PerangkatActivity"
            android:label="Perangkat tertaut" />
        <activity
            android:name=".PesanActivity"
            android:label="Pesan berbintang" />
        <activity
            android:name=".SetelanActivity"
            android:label="Setelan" />

    </application>

</manifest>